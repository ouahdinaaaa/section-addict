{% schema %}
{
  "name": "Black Friday Banner",
  "settings": [
    {
      "type": "text",
      "id": "main_title",
      "label": "Titre principal",
      "default": "Black Friday"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Sous-titre",
      "default": "Shop Now"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Couleur de fond",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Couleur du texte",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "highlight_color",
      "label": "Couleur Black Friday",
      "default": "#FFD700"
    },
    {
      "type": "range",
      "id": "font_size",
      "label": "Taille du texte (rem)",
      "default": 3,
      "min": 1,
      "max": 10,
      "step": 0.1
    },
    {
      "type": "range",
      "id": "section_width",
      "label": "Largeur de la section (%)",
      "default": 100,
      "min": 50,
      "max": 100,
      "step": 1
    }
  ],
  "presets": [
    {
      "name": "Black Friday Banner",
      "category": "Promotion"
    }
  ]
}
{% endschema %}



<style>

:root {
  --color-black: black;
  --color-white: white;
  --color-yellow: yellow;
  --color-grey: #e8e8e8;
  --time: 4s;
  --time-s: 0.3s;
}

/* base */
* { box-sizing: border-box; }

a { text-decoration: none; color: inherit; }


/* container centré */
.container2 {
  width: 100%;
  margin: 0 auto;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: wrap;
    font-family: "Montserrat", sans-serif;
  background-color: black;
  color: #fff;
  font-size: 3rem;      /* <-- règle importante restaurée */
  line-height: 1;
  font-weight: 300;
  display: flex;
  justify-content: center;
  align-items: center;
  margin: 0;
}

/* règles communes aux boxes (taille/ratio) */
.box {
  cursor: pointer;
  position: relative;
  display: inline-block;
  width: 30vw;          /* valeur par défaut (petite boîte) */
  padding-bottom: 30vw; /* crée le ratio (square-like) */
  margin: 1vw;
  text-transform: uppercase;
  font-weight: 800;
  overflow: hidden;
}
@media (max-width: 768px) {
  .box {
    width: 46vw;
    padding-bottom: 46vw;
    font-size : 2rem
  }
}

@media (max-width: 478px) {
  .box {
    width: 46vw;
    padding-bottom: 46vw;
    font-size : 1.3rem
  }
}

/* contenu interne */
.box__content {
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  padding: 2vw;
  display: flex;
  flex-wrap: wrap;
  font-size: x-large;
  justify-content: space-between;
  align-items: center;
  align-content: center;
}

/* titre animé */
.box__title {
  transition: all var(--time-s);
  transform: scale(1) translateY(0);
}

/* --- SPÉCIFIQUE À LA BOX 4 --- */
/* la largeur/ratio spécifiques à .box--4 (comme dans l'original) */
.box--1, .box--4 {
  width: 100%;
}
@media (max-width: 768px) {
  .box--1, .box--4 {
    width: 100%;
    padding-bottom: 46vw;
  }

  .box__content {
    font-size : 5vh;
  }
}

/* visuel box 4 */
.box--4 {
  background-color: var(--color-black);
  text-shadow: -1px 0 0 var(--color-white),
               0 -1px 0 var(--color-white),
               1px 0 0 var(--color-white),
               0 1px 0 var(--color-white);
  color: #000;
}

/* animation des spans */
.box--4 span {
  letter-spacing: 0.3vw;
  animation: text var(--time) linear infinite running;
}
.box--4 span:nth-child(2n+1) {
  animation-delay: calc(var(--time) / 4);
}
.box--4 span:nth-child(3n+2) {
  animation-delay: calc(var(--time) / 2);
}

/* hover states */
.box--4:hover {
  text-shadow: -1px 0 0 var(--color-grey),
               0 -1px 0 var(--color-grey),
               1px 0 0 var(--color-grey),
               0 1px 0 var(--color-grey);
}
.box--4:hover span {
  animation: none;
}
.box--4:hover .box__title {
  z-index: 2;
  opacity: 1;
  text-shadow: -1px 0 0 var(--color-yellow),
               0 -1px 0 var(--color-yellow),
               1px 0 0 var(--color-yellow),
               0 1px 0 var(--color-yellow);
  transform: scale(1.6);
  color: var(--color-yellow);
}
.box--4:hover .box__title--top {
  transform: scale(1.6) translateY(-1vw);
}

/* keyframes */
@keyframes text {
  0%, 19% { color: var(--color-white); }
  20%, 100% { color: var(--color-black); }
}
</style>


<div class="container2">
  <a class="box box--4">
    <div class="box__content">
      70%
      <span>WEEKEND SALE EVENT</span>
      5%
      <span>the full</span>
      20%
      <span>SUPER SALE</span>
      10%
      <span>5% off</span>
      <span>SALE</span>
      70%
      <span>SALE EVENT</span>
      <span>ONE Day</span>!
      10%
      <span>SALE</span>
      <span>the full</span>
      70%! $$
      <span class="box__title box__title--top">Black Friday</span>
      <span>70%</span>$$!
      <span>85% off</span>
      <span class="box__title">shop now</span>$
      <span>5% off</span>$
      <span>50% OFF</span>
      <span>SALE</span>
      70%
      <span>BEST OFFER</span>
      <span>the full</span>$
      30%!
      <span>Limited time</span>
      50%<span>off sale</span>
      <span>special OFFER</span>
    </div>
  </a>
</div>